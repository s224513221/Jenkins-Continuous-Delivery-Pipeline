pipeline {

    agent any

    environment {
        DIRECTORY_PATH = "${env.WORKSPACE}"
        TESTING_ENVIRONMENT = 'staging environment'
        PRODUCTION_ENVIRONMENT = 'liz'
    }

    stages {

        stage("Template") {

            steps {
                echo "Template"
            }

        }

    }

}

